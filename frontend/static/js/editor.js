// Transcript editor interactions (stub).

const EditorUI = {
  init() {
    // load minute text, enable edits, autosave
  },
};

document.addEventListener("DOMContentLoaded", () => EditorUI.init());

